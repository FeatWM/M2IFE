# Third-party notices

M2-IFE uses Ultralytics YOLO for the detector. The inspected local Ultralytics
checkout is distributed under AGPL-3.0. Review the current Ultralytics licensing
terms before publishing or deploying the project.

The classifier uses PyTorch and torchvision model definitions for VGG16,
ResNet18, and ConvNeXt-Large. Their respective package licenses continue to
apply.

The project owner must select and add the repository's own `LICENSE` file before
the repository is made public.

